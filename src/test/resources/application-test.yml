spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coffee_shop_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  sql:
    init:
      mode: never

app:
  scheduling:
    enabled: false
